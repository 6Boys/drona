# deps
node_modules/
.pnp
.pnp.js

# env — never commit real DB links
.env
.env.local
.env.*.local
!.env.example

# next
web/.next/
web/out/
web/next-env.d.ts
web/coverage/

# go
api/bin/
api/tmp/
*.test
api/coverage.out

# uploaded pictures from a local run (MEDIA_DIR defaults to ./data/media);
# in Docker these live in the `media` volume instead
api/data/
data/

# scripts/backup.sh output — real student data, never commit it
backups/

# prisma
prisma/generated/
prisma/.env

# os / editor
.DS_Store
*.log
.idea/
.vscode/
.claude/

